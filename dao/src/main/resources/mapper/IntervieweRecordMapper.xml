<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaolintt.mapper.InterviewRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xiaolintt.po.InterviewRecord">
        <id column="id" property="id" />
        <result column="candidate_id" property="candidateId" />
        <result column="job_name" property="jobName" />
        <result column="answer_content" property="answerContent" />
        <result column="take_time" property="takeTime" />
        <result column="result" property="result" />
        <result column="create_time" property="createTime" />
        <result column="updated_time" property="updatedTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, candidate_id, job_name, answer_content, take_time, result, create_time, updated_time
    </sql>

    <select id="queryInterviewRecordLibList" resultType="com.xiaolintt.vo.InterviewRecordVO">
        SELECT
        ir.id as interviewRecordId,
        c.id as candidateId,
        c.real_name as realName,
        c.identity_num as identityNum,
        c.mobile as mobile,
        c.sex as sex,
        c.email as email,
        c.job_id as jobId,
        ir.create_time as createTime,
        ir.result as result,
        ir.answer_content as answerContent,
        ir.take_time as takeTime,
        j.job_name as jobName
        FROM
        interview_record ir
        LEFT JOIN
        candidate c
        ON
        c.id = ir.candidate_id
        LEFT JOIN
        job j
        ON
        c.job_id = j.id
        WHERE
        1 = 1
        <if test="realName != null and realName != ''">
            AND c.real_name LIKE concat('%', #{realName},'%')
        </if>
        <if test="mobile != null and mobile != ''">
            AND c.mobile LIKE concat('%', #{mobile},'%')
        </if>
        ORDER BY
        ir.create_time
        DESC
    </select>


</mapper>
