<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaolintt.mapper.CandidateMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xiaolintt.po.Candidate">
        <id column="id" property="id" />
        <result column="real_name" property="realName" />
        <result column="identity_num" property="identityNum" />
        <result column="mobile" property="mobile" />
        <result column="sex" property="sex" />
        <result column="face" property="face" />
        <result column="email" property="email" />
        <result column="birthday" property="birthday" />
        <result column="country" property="country" />
        <result column="province" property="province" />
        <result column="city" property="city" />
        <result column="district" property="district" />
        <result column="address" property="address" />
        <result column="job_id" property="jobId" />
        <result column="remark" property="remark" />
        <result column="created_time" property="createdTime" />
        <result column="updated_time" property="updatedTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, real_name, identity_num, mobile, sex, face, email, birthday, country, province, city, district, address, job_id, remark, created_time, updated_time
    </sql>

    <select id="queryCandidateLibList" resultType="com.xiaolintt.vo.CandidateVO">
        SELECT
        c.id as candidateId,
        c.real_name as realName,
        c.identity_num as identityNum,
        c.mobile as mobile,
        c.sex as sex,
        c.email as email,
        c.birthday as birthday,
        c.job_id as jobId,
        c.created_time as createdTime,
        j.job_name as jobName
        FROM
        candidate c
        LEFT JOIN
        job j
        on
        c.job_id = j.id
        WHERE
        1=1
        <if test="realName != null and realName !=''">
            and c.real_name LIKE concat('%',#{realName},'%')
        </if>
        <if test="mobile != null and mobile !=''">
            and c.mobile LIKE concat('%',#{mobile},'%')
        </if>
        ORDER BY
        c.updated_time
        DESC
    </select>


</mapper>
